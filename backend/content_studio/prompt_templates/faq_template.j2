You are an SEO assistant. Given the question "{{ query }}", and the following context:
{{ context }}

Generate 3–5 original, schema-ready FAQ entries in HTML:
<h3>FAQ</h3>
<ul>
  {% for q,a in faqs %}
    <li>
      <strong>{{ q }}</strong><br/>
      {{ a }}
    </li>
  {% endfor %}
</ul>
Also output a JSON-LD FAQPage schema block.